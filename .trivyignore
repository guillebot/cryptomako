# Document accepted findings here with a short reason and expiry/review date.
# Example:
# CVE-2024-12345 exp:2026-01-01 # dev-only fixture dependency, no fix yet

# SPM checkouts under .build/ are excluded via --skip-dirs (not shipped).
# Upstream Dockerfiles and benchmark private keys live there.